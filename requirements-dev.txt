# Python dev tools, exact pins so VSCode, the container and a host CLI agree.
# Installed into /opt/dev-tools in the image (Dockerfile) and into .venv on a host.
# The shell tools are pinned binaries, not pip packages — see Dockerfile / CI:
#   shfmt v3.13.1, shellcheck v0.10.0
ruff==0.16.0
mypy==2.3.0
